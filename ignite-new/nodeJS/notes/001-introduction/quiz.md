## Quiz

**1 - De acordo com a aula, como habilitamos o uso das importações utilizando o ESModules?**

O Adicionando no arquivo `package.json` a propriedade `"type"` com o valor de `"module"`

**2 - O prefixo node: na importação de um módulo serve para informar que esse módulo é interno do Node.js:**

Verdadeiro

**3 - Para que serve o módulo http do Node.js?**

Para criar e lidar com as requisições HTTP em uma porta específica

**4 - Sobre o parâmetro request é possível afirmar que: Através desse objeto é possível obter todas as informações presente na requisição, como dados enviados em JSON, parâmetros de rota nomeados e não nomeados, entre outros.**

Verdadeiro